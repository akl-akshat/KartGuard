# Phase 4 stub — replaced by the real LLM root-cause diagnoser in Phase 5.
from agent.state import ResolutionState


def diagnosis(state: ResolutionState) -> dict:
    return {"root_cause": "genuine_other"}
