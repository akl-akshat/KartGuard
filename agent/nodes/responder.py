# Phase 4 stub — replaced by the real LLM responder in Phase 7.
from agent.state import ResolutionState


def responder(state: ResolutionState) -> dict:
    return {"customer_message": "Thanks for reaching out — we are processing your request."}
