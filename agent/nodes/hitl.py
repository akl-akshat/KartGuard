# Phase 4 stub — replaced by the real interrupt/resume HITL node in Phase 7.
from agent.state import ResolutionState


def hitl(state: ResolutionState) -> dict:
    return {"human_decision": "approve"}
